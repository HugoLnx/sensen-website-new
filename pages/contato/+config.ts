import type { Config } from "vike/types";

export default {
  // Metadados para a Página de Contato
  titleKey: "header.contact",
  descriptionKey: "contact.subtitle",
  ogSiteName: "header.contact"
} satisfies Config;
